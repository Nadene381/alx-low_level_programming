#include <stdio.h>
/**
 * main - Program that uses printf function to print a string
 * Return: 0
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,");
return (0);
}
