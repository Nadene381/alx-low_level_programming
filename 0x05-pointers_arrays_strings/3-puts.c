#include "main.h"
/**
 * _puts - Check main code
 * Description: Function that prints a string
 * Return: 1
 * @str: variable passed in fuction
 */
void _puts(char *str);
{
while (*str != '\0') 
{
putchar(*str);
str++;
}
}
